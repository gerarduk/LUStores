// Simple test without complex imports
describe('Basic Test', () => {
  it('should run a basic test', () => {
    expect(1 + 1).toBe(2);
  });

  it('should test basic math operations', () => {
    expect(2 * 3).toBe(6);
    expect(10 / 2).toBe(5);
  });
});
